// apps/frontend/src/native/ARKitViewNativeComponent.ts
// RN 0.82 Fabric component spec for ARKitView

import codegenNativeComponent from 'react-native/Libraries/Utilities/codegenNativeComponent';

export interface ARKitViewProps {
  // add props later if needed
}

export default codegenNativeComponent<ARKitViewProps>('ARKitView');
